{
  "name": "proofpost-backend",
  "version": "0.1.0",
  "private": true,
  "description": "ProofPost backend: content generation and proof bundles for CROO/CAP.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "provider": "tsx src/provider.ts",
    "requester": "tsx examples/requester.ts"
  },
  "dependencies": {
    "@croo-network/sdk": "^0.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3",
    "vitest": "^2.1.8"
  }
}